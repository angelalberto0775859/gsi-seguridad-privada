import type { Language } from './sitePreferences'

export const languageLabels: Record<Language, string> = {
  es: 'ES',
  en: 'EN',
  zh: '中文',
}

export const supportedLanguages: Language[] = ['es', 'en', 'zh']
